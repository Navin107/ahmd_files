import os
import subprocess
from subprocess import call,Popen,PIPE

# cmd="dir"
# process = Popen(cmd, shell=True)
# process.communicate ()
subprocess.call('cd ahmd_files', shell=True)

# cmd = "git status"
# process = Popen(cmd, shell=True)
# process.communicate ()

# cmd = "git add . "
# process = Popen(cmd, shell=True)
# process.communicate ()

# cmd = 'git commit -m "newfiles added"'
# process = Popen(cmd, shell=True)
# process.communicate ()


